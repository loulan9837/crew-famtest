# 用例格式优化方案 · 仅修改 Agent 定义与 Task 交互

> **目标**：解决单元格换行无效、用例概述/前置条件/测试步骤之间的场景重复问题。  
> **原则**：以 `config/agents.yaml` 与 `crew_test.py` 导出为准；下文 **§2.1 预期结果** 已与主仓对齐（2026 起）。

---

## 一、问题与根因

| 问题 | 根因 |
|------|------|
| 1. 单元格内换行在 Excel 中显示不佳 | 需在导出层将 `<br>` 转为 `\n`，并将「1.xxx；2.yyy」中分号后接「数字.」转为换行，同单元格多行可复制 |
| 2. 用例概述已写明场景时，前置条件仍重复写场景 | Agent 未约束「用例概述与前置条件」的职责边界，导致冗余 |
| 3. 前置条件已有场景时，测试步骤仍重复写场景 | Agent 未约束「前置条件与测试步骤」的衔接，导致步骤冗余 |

---

## 二、优化方案

### 2.1 预期结果：同单元格「1.xxx；2.yyy」+ 允许 `<br>`（已落地）

**当前约定（`config/agents.yaml` + `crew_test._sanitize_cell_for_excel`）**：

- **多条**预期：同单元格 **「1.xxx；2.yyy」** 全角分号串联，**单条**不得加「1.」前缀。
- **不禁止** `<br>`：可在串联前后或条款之间追加 `<br>` 排版；**禁止** Markdown 表格单元格内**裸回车**（避免拆表）。
- **导出**：`<br>` → 单元格内 `\n`；`；` 后接 `\d+\.` → `\n` + 该序号，便于 Excel 同单元格多行显示与复制。

以下历史草案（曾建议「禁止 `<br>`」）**已废弃**，以主仓为准。

---

### 2.2 用例概述与前置条件：避免场景重复

**原则**：用例概述负责「场景/对象定位」，前置条件仅补充「该场景下未涵盖的其它必要条件」。

**修改点**：

- **case_agent.backstory** 中「前置条件」准则：
  - 新增：`若用例概述已写明场景（如「直播间-礼物面板-余额不足态」），前置条件仅补充该场景下未涵盖的其它必要条件（如账号、网络、数据状态），禁止重复描述场景本身`

- **organize_agent.backstory** 中拆解原则（可选，用于 2 号输出更清晰）：
  - 新增：`用例概述采用「大模块-小组件-具体元素/状态」，前置条件由 3 号根据概述补充，不重复场景`

---

### 2.3 前置条件与测试步骤：避免步骤重复场景

**原则**：前置条件已描述的场景，测试步骤从「达成该场景后的第一个操作动作」开始，不再重复场景。

**修改点**：

- **case_agent.backstory** 中「测试步骤」准则：
  - 新增：`若前置条件已写明场景或初始状态，测试步骤从达成该场景后的第一个操作动作开始，禁止在步骤中重复描述「进入xxx页面」「确保xxx状态」等前置内容`

- **task3.description** 中任务执行说明：
  - 新增：`前置条件与测试步骤禁止重复：前置条件已有的场景/状态，测试步骤中不再复述，直接从第一个操作动作开始`

---

## 三、agents.yaml 落点（权威）

预期结果与 task3/task4 文案以仓库 **`config/agents.yaml`** 为准，勿再按本节下方历史代码块实施（旧块曾含「禁止 `<br>`」，已废弃）。

| 落点 | 内容 |
|------|------|
| `case_agent` → `backstory` → 字段撰写准则 | 预期结果：`1.xxx；2.yyy` 全角分号串联、单条禁「1.」、**允许** `<br>` |
| `review_agent` → `backstory` → 四大验收标准之 2 | 同上 + 禁裸回车拆表 |
| `task3` → `description` → 生死红线 4～5 | 禁裸回车；允许 `<br>`；预期结果多条须分号串联 |
| `task4` → `description` → 字段规范 | 校验分号串联、单条禁「1.」、允许 `<br>`、禁裸回车 |

---

## 四、导出与 Excel（`crew_test.py`）

`_sanitize_cell_for_excel` 已实现：

- `<br>`（大小写不敏感）→ 单元格内 `\n`
- 全角 `；` 后接 `数字.` → `\n` + 该序号（与「1.xxx；2.yyy」列举对齐）

---

## 五、验收预期

| 验收项 | 预期 |
|--------|------|
| 预期结果多条 | 同单元格「1.xxx；2.yyy」；可选 `<br>`；导出 Excel 同单元格多行可读 |
| 预期结果单条 | 无「1.」前缀 |
| 用例概述含场景 | 前置条件不重复场景（若已按 §2.2 合并） |
| 审查 | `review_agent` 按 YAML 字段规范句验收 |
